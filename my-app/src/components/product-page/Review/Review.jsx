import styles from "./Review.module.css"

export default function Review({feedback:{customerName, text, createdDate, rate}}) {
    return (
        <div className={styles.container}>
            
        </div>
    );
}